# Infrastructure config only. LLM API keys are never read from env/config -
# every visitor enters their own in the app (Settings, or inline when picking a
# model). See session_keys.py.
DATABASE_URL="postgresql://user:password@host:5432/dbname"
